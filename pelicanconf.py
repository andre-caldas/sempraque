import os
import sys

sys.path.append(os.curdir)
from commonconf import *

# Uncomment following line if you want document-relative URLs in debug
# RELATIVE_URLS = True

# Here, settings used only during debug.
